#!/bin/bash

path_to_lattice_model_implementations="./../../"
path_to_lattice_model_implementations="$(cd "$path_to_lattice_model_implementations" && pwd -P)"
source "${path_to_lattice_model_implementations}/bash_scripts/build_simulation.sh"
